node_modules/
dist/
test-results/
playwright-report/
# MediaPipe WASM is copied from node_modules at predev/prebuild (copy-vision-assets.mjs);
# the hand_landmarker.task model is committed (not re-derivable offline).
apps/web/public/models/mediapipe/wasm/
.venv/
__pycache__/
*.pyc
.DS_Store
# WP-5: opt-in clip/session uploads + persisted cost-cap budget (local only).
services/backend/storage/
# WP-6 public-data bootstrap (scripts/fetch-eval-data.mjs) — redownloadable,
# not committed; the manifest it writes documents provenance/license/sha256.
data/eval/

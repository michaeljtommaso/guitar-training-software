# Blockers Ledger

Entries are appended as they are found during the overnight run.

| Blocker | WP | Why blocked | Assumption made to proceed | Human action needed |
| --- | --- | --- | --- | --- |
| Docker MISSING on build machine | WP-5 | Dockerfile will ship but cannot be built/run locally tonight | Backend runs via uvicorn directly; Dockerfile authored untested | Install Docker Desktop to validate container build |
| gh CLI MISSING | WP-0 | CI YAML cannot be exercised on GitHub tonight | CI steps mirrored as local commands, all run locally | Push to GitHub and confirm Actions run green |
| corepack EPERM (Program Files not writable) | WP-0 | corepack shim install failed | pnpm installed via `npm i -g pnpm` instead | none — cosmetic |
| Sustained-30fps + no-jank gate unverifiable headless | WP-1 | Chromium fake camera delivers ~20fps; jank needs a human eye | E2E proves plumbing at fake-cam rate; threshold set to >25 ticks/2s | Human: run `pnpm dev`, open the app with a real webcam, confirm 30fps preview and no jank |
| Basic Pitch ORT path deferred | WP-2 | No validated Basic Pitch ONNX export within timebox | Real @spotify/basic-pitch (TF.js, Apache-2.0) wired behind NoteSource seam | Export/validate ONNX, re-add onnxruntime-web, wire WebGPU EP |
| CREPE tuner deferred | WP-2 | onnxcrepe needs a manual ONNX export step | Own YIN DSP behind TunerSource, <5 cents on synthetic sines | Run the CREPE ONNX export, swap in behind TunerSource |
| Audio accuracy gates unmeasured | WP-2 | No real guitar recordings tonight | Synthetic fixtures only, honestly labeled | Record/eval on GuitarSet + home-mic clips before claiming ≥90% / <100ms |
| Fingertip accuracy gate unmeasured | WP-3 | No real webcam+marker captures tonight | Geometry proven on synthetic fixtures only | Do a seated+ChArUco capture session, measure ≥85% gate |
| ~~docs §9.4 lesson example uses inconsistent string numbering~~ RESOLVED 2026-07-03 (62cda31) | WP-4 | avoid_strings:[1] contradicted its own fingerings | Resolved code-wide to standard (1=high e) at cc42d15 | Done — docs example fixed to expected [1-5] / avoid [6] |
| False-critical <5% + calibration gates unmeasured | WP-4 | No labeled internal lesson set exists | Fusion logic proven on synthetic streams; false-positive-averse gates unit-enforced | Record + label real lesson sessions (WP-6 tooling), then measure |
| <250ms hint gate not glass-to-glass | WP-4 | Headless e2e cannot see glass | Main-thread ingest→hint-emit measured (~0.2-1.3ms median) and labeled as such | Measure glass-to-glass on reference laptop with real camera/mic |
| No frontier API key — MITIGATED 2026-07-04 for personal use | WP-5 | Anthropic API adapter complete but never exercised live | **Subscription mode now live-proven**: `COACH_PROVIDER=claude_cli` runs coaching off the owner's Claude subscription via the local CLI (validated live twice, 363dac2) — no key needed for personal use | API key only needed for the multi-user path: set ANTHROPIC_API_KEY, smoke the `anthropic` adapter live, then run the teacher-agreement eval |
| Teacher-agreement gate (>75%) unmeasured | WP-5 | No teacher raters tonight | Bounded-taxonomy output + template fallback verified mechanically | Recruit 2-3 guitar teachers, run the eval per §16 |
| Trainable labeled set does not exist | WP-6 | No capture sessions run | Tooling proven on synthetic/sample fixtures + GuitarSet | Execute data/capture-protocol.md stage 1 (controlled scripted-error sessions), label in the annotation tool |
| IDMT-SMT-GUITAR not fetched | WP-6 | CC BY-NC-ND + form-gated distribution | Documented in manifest; GuitarSet (CC-BY-4.0) fetched instead | If wanted for offline eval only: request via Fraunhofer form (never ship anything derived from it) |
| Tauri desktop target NOT built | WP-7 | No Rust toolchain on this machine (`cargo`/`rustc` absent) | Web MVP is the shippable target tonight; desktop deferred per ADR-001 | Install Rust + tauri-cli, scaffold `apps/desktop` pointing at the web build, then re-run §16 gates on desktop |
| Cloud sync/auth layer NOT built | WP-7 | No cloud project tonight; provider decided 2026-07-03: **Firebase** (owner call, ADR-010 amended — was Supabase) | Sync deferred; design per amended ADR-010 (Firebase Auth + Firestore + Cloud Storage, per-user rules, opt-in, Local-only mode never syncs; FastAPI proxy unchanged) | Create a Firebase project, then build the opt-in sync layer and prove it round-trips without leaking in Local-only mode |
| WP-7 gate (§16 metrics on web AND Tauri) unmeasurable | WP-7 | No Tauri build + §16 gates unmeasured on real hardware (see WP-2/3/4 rows) | Sentry privacy wiring, latency histograms, complaint metric, and eval-smoke regression gate all landed + unit/CI-tested; the §16 acceptance gate itself is DEFERRED | Ratify a reference laptop (infra/reference-hardware.md), measure §16 on web, then repeat on the Tauri build |
| Sentry never exercised against a live DSN | WP-7 | No Sentry DSN tonight (privacy-first, opt-in) | Init is DSN-gated: unit-enforced that no import/init/network happens without VITE_SENTRY_DSN; scrub + masked-replay + no-leak ErrorBoundary are unit-tested pure/local | Set VITE_SENTRY_DSN in a real project, confirm masked on-error replay contains no un-masked biometric media |
| e2e job not yet run on a GitHub runner | WP-7 | `gh` CLI absent; CI never executed remotely tonight | e2e job authored (non-blocking, `playwright install --with-deps chromium`, fake-device flags); passes locally | Push to GitHub, confirm the e2e job goes green on ubuntu-latest, then promote it to a required check |

#!/usr/bin/env node
// ponytail: STUB — the real dependency-license gate (fail CI on any
// AGPL/NC/GPL package reaching the shipped client bundle, per ADR-011) lands
// in WP-0b. This stub exists only so CI has a license-check step to wire up
// now.
console.log("license-check: STUB — real gate lands in WP-0b");
process.exit(0);
